See the root README.md for setup, architecture, schema, and API docs.

Frontend: Next.js app. Run `npm run dev` after the API is on port 8000.

Copy `.env.example` to `.env.local` if you need to override `NEXT_PUBLIC_API_URL`.

